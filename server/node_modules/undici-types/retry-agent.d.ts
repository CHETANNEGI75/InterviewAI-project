import Dispatcher from 'server/node_modules/undici-types/dispatcher'
import RetryHandler from 'server/node_modules/undici-types/retry-handler'

export default RetryAgent

declare class RetryAgent extends Dispatcher {
  constructor (dispatcher: Dispatcher, options?: RetryHandler.RetryOptions)
}
